s(:begin,
  s(:send, nil, :require,
    s(:str, "test/unit")),
  s(:send, nil, :require,
    s(:str, "logger")),
  s(:class,
    s(:const, nil, :TestLoggerSeverity),
    s(:const,
      s(:const,
        s(:const, nil, :Test), :Unit), :TestCase),
    s(:def, :test_enum,
      s(:args),
      s(:begin,
        s(:lvasgn, :logger_levels,
          s(:send,
            s(:const, nil, :Logger), :constants)),
        s(:lvasgn, :levels,
          s(:array,
            s(:str, "WARN"),
            s(:str, "UNKNOWN"),
            s(:str, "INFO"),
            s(:str, "FATAL"),
            s(:str, "DEBUG"),
            s(:str, "ERROR"))),
        s(:block,
          s(:send,
            s(:send,
              s(:const,
                s(:const, nil, :Logger), :Severity), :constants), :each),
          s(:args,
            s(:arg, :level)),
          s(:begin,
            s(:send, nil, :assert,
              s(:send,
                s(:lvar, :levels), :include?,
                s(:send,
                  s(:lvar, :level), :to_s))),
            s(:send, nil, :assert,
              s(:send,
                s(:lvar, :logger_levels), :include?,
                s(:lvar, :level))))),
        s(:send, nil, :assert_equal,
          s(:send,
            s(:lvar, :levels), :size),
          s(:send,
            s(:send,
              s(:const,
                s(:const, nil, :Logger), :Severity), :constants), :size))))))
