s(:begin,
  s(:send, nil, :require,
    s(:str, "rubygems/test_case")),
  s(:class,
    s(:const, nil, :TestGemResolverRequirementList),
    s(:const,
      s(:const, nil, :Gem), :TestCase),
    s(:begin,
      s(:def, :setup,
        s(:args),
        s(:begin,
          s(:zsuper),
          s(:ivasgn, :@list,
            s(:send,
              s(:const,
                s(:const,
                  s(:const, nil, :Gem), :Resolver), :RequirementList), :new)))),
      s(:def, :test_each,
        s(:args),
        s(:begin,
          s(:lvasgn, :dep,
            s(:send,
              s(:const,
                s(:const, nil, :Gem), :Dependency), :new,
              s(:str, "a"),
              s(:str, "= 1"))),
          s(:lvasgn, :req,
            s(:send,
              s(:const,
                s(:const,
                  s(:const, nil, :Gem), :Resolver), :DependencyRequest), :new,
              s(:lvar, :dep),
              s(:nil))),
          s(:send,
            s(:ivar, :@list), :add,
            s(:lvar, :req)),
          s(:send, nil, :assert_equal,
            s(:array,
              s(:lvar, :req)),
            s(:send,
              s(:send,
                s(:ivar, :@list), :each), :to_a)))))))
