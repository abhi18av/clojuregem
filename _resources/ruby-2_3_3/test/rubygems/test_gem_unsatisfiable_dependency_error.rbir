s(:begin,
  s(:send, nil, :require,
    s(:str, "rubygems/test_case")),
  s(:class,
    s(:const, nil, :TestGemUnsatisfiableDependencyError),
    s(:const,
      s(:const, nil, :Gem), :TestCase),
    s(:begin,
      s(:def, :setup,
        s(:args),
        s(:begin,
          s(:zsuper),
          s(:ivasgn, :@a_dep,
            s(:send, nil, :dep,
              s(:str, "a"),
              s(:str, "~> 1"))),
          s(:ivasgn, :@req,
            s(:send,
              s(:const,
                s(:const,
                  s(:const, nil, :Gem), :Resolver), :DependencyRequest), :new,
              s(:ivar, :@a_dep),
              s(:nil))),
          s(:ivasgn, :@e,
            s(:send,
              s(:const,
                s(:const, nil, :Gem), :UnsatisfiableDependencyError), :new,
              s(:ivar, :@req))))),
      s(:def, :test_errors,
        s(:args),
        s(:begin,
          s(:send, nil, :assert_equal,
            s(:array),
            s(:send,
              s(:ivar, :@e), :errors)),
          s(:send,
            s(:send,
              s(:ivar, :@e), :errors), :<<,
            s(:sym, :a)),
          s(:send, nil, :assert_equal,
            s(:array,
              s(:sym, :a)),
            s(:send,
              s(:ivar, :@e), :errors)))),
      s(:def, :test_name,
        s(:args),
        s(:send, nil, :assert_equal,
          s(:str, "a"),
          s(:send,
            s(:ivar, :@e), :name))),
      s(:def, :test_version,
        s(:args),
        s(:send, nil, :assert_equal,
          s(:send,
            s(:ivar, :@a_dep), :requirement),
          s(:send,
            s(:ivar, :@e), :version))))))
