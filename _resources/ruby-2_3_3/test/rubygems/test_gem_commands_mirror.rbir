s(:begin,
  s(:send, nil, :require,
    s(:str, "rubygems/test_case")),
  s(:send, nil, :require,
    s(:str, "rubygems/commands/mirror_command")),
  s(:class,
    s(:const, nil, :TestGemCommandsMirrorCommand),
    s(:const,
      s(:const, nil, :Gem), :TestCase),
    s(:begin,
      s(:def, :setup,
        s(:args),
        s(:begin,
          s(:zsuper),
          s(:ivasgn, :@cmd,
            s(:send,
              s(:const,
                s(:const,
                  s(:const, nil, :Gem), :Commands), :MirrorCommand), :new)))),
      s(:def, :test_execute,
        s(:args),
        s(:begin,
          s(:block,
            s(:send, nil, :use_ui,
              s(:ivar, :@ui)),
            s(:args),
            s(:send,
              s(:ivar, :@cmd), :execute)),
          s(:send, nil, :assert_match,
            s(:regexp,
              s(:str, "Install the rubygems-mirror"),
              s(:regopt, :i)),
            s(:send,
              s(:ivar, :@ui), :error)))))))
