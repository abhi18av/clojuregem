s(:begin,
  s(:send, nil, :require,
    s(:str, "scanf")),
  s(:class,
    s(:const, nil, :TestScanfIO),
    s(:const,
      s(:const,
        s(:const, nil, :Test), :Unit), :TestCase),
    s(:def, :test_io,
      s(:args),
      s(:ensure,
        s(:begin,
          s(:lvasgn, :fh,
            s(:send,
              s(:const, nil, :File), :new,
              s(:send,
                s(:const, nil, :File), :join,
                s(:send,
                  s(:const, nil, :File), :dirname,
                  s(:str, "(string)")),
                s(:str, "data.txt")),
              s(:str, "r"))),
          s(:send, nil, :assert_equal,
            s(:int, 0),
            s(:send,
              s(:lvar, :fh), :pos)),
          s(:send, nil, :assert_equal,
            s(:array,
              s(:str, "this"),
              s(:str, "is")),
            s(:send,
              s(:lvar, :fh), :scanf,
              s(:str, "%s%s"))),
          s(:send, nil, :assert_equal,
            s(:array,
              s(:int, 33),
              s(:str, "little")),
            s(:send,
              s(:lvar, :fh), :scanf,
              s(:str, "%da fun%s")))),
        s(:send,
          s(:lvar, :fh), :close)))))
