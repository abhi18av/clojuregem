s(:begin,
  s(:send, nil, :require,
    s(:send,
      s(:const, nil, :File), :expand_path,
      s(:str, "../xref_test_case"),
      s(:str, "(string)"))),
  s(:class,
    s(:const, nil, :TestRDocAlias),
    s(:const, nil, :XrefTestCase),
    s(:def, :test_to_s,
      s(:args),
      s(:begin,
        s(:lvasgn, :a,
          s(:send,
            s(:const,
              s(:const, nil, :RDoc), :Alias), :new,
            s(:nil),
            s(:str, "a"),
            s(:str, "b"),
            s(:str, ""))),
        s(:send,
          s(:lvar, :a), :parent=,
          s(:ivar, :@c2)),
        s(:send, nil, :assert_equal,
          s(:str, "alias: b -> #a in: RDoc::NormalClass C2 < Object"),
          s(:send,
            s(:lvar, :a), :to_s))))))
