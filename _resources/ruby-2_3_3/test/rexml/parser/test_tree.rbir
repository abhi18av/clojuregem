s(:begin,
  s(:send, nil, :require,
    s(:str, "test/unit")),
  s(:send, nil, :require,
    s(:str, "rexml/document")),
  s(:send, nil, :require,
    s(:str, "rexml/parsers/treeparser")),
  s(:module,
    s(:const, nil, :REXMLTests),
    s(:class,
      s(:const, nil, :TestTreeParser),
      s(:const,
        s(:const,
          s(:const, nil, :Test), :Unit), :TestCase),
      s(:class,
        s(:const, nil, :TestInvalid),
        s(:self),
        s(:begin,
          s(:def, :test_unmatched_close_tag,
            s(:args),
            s(:begin,
              s(:lvasgn, :xml,
                s(:str, "<root></not-root>")),
              s(:lvasgn, :exception,
                s(:block,
                  s(:send, nil, :assert_raise,
                    s(:const,
                      s(:const, nil, :REXML), :ParseException)),
                  s(:args),
                  s(:send, nil, :parse,
                    s(:lvar, :xml)))),
              s(:send, nil, :assert_equal,
                s(:dstr,
                  s(:str, "Missing end tag for 'root' (got \"not-root\")\n"),
                  s(:str, "Line: 1\n"),
                  s(:str, "Position: "),
                  s(:begin,
                    s(:send,
                      s(:lvar, :xml), :bytesize)),
                  s(:str, "\n"),
                  s(:str, "Last 80 unconsumed characters:\n")),
                s(:send,
                  s(:lvar, :exception), :to_s)))),
          s(:def, :test_no_close_tag,
            s(:args),
            s(:begin,
              s(:lvasgn, :xml,
                s(:str, "<root>")),
              s(:lvasgn, :exception,
                s(:block,
                  s(:send, nil, :assert_raise,
                    s(:const,
                      s(:const, nil, :REXML), :ParseException)),
                  s(:args),
                  s(:send, nil, :parse,
                    s(:lvar, :xml)))),
              s(:send, nil, :assert_equal,
                s(:dstr,
                  s(:str, "No close tag for /root\n"),
                  s(:str, "Line: 1\n"),
                  s(:str, "Position: "),
                  s(:begin,
                    s(:send,
                      s(:lvar, :xml), :bytesize)),
                  s(:str, "\n"),
                  s(:str, "Last 80 unconsumed characters:\n")),
                s(:send,
                  s(:lvar, :exception), :to_s)))),
          s(:send, nil, :private),
          s(:def, :parse,
            s(:args,
              s(:arg, :xml)),
            s(:begin,
              s(:lvasgn, :document,
                s(:send,
                  s(:const,
                    s(:const, nil, :REXML), :Document), :new)),
              s(:lvasgn, :parser,
                s(:send,
                  s(:const,
                    s(:const,
                      s(:const, nil, :REXML), :Parsers), :TreeParser), :new,
                  s(:lvar, :xml),
                  s(:lvar, :document))),
              s(:send,
                s(:lvar, :parser), :parse))))))))
