s(:begin,
  s(:send, nil, :require,
    s(:str, "test/unit")),
  s(:send, nil, :require,
    s(:str, "rexml/document")),
  s(:module,
    s(:const, nil, :REXMLTests),
    s(:class,
      s(:const, nil, :OrderTester),
      s(:const,
        s(:const,
          s(:const, nil, :Test), :Unit), :TestCase),
      s(:begin,
        s(:casgn, nil, :DOC,
          s(:dstr,
            s(:str, "<paper>\n"),
            s(:str, "<title>Remove this element and figs order differently</title>\n"),
            s(:str, "<figure src=\"fig1\"/>\n"),
            s(:str, "<figure src=\"fig2\"/>\n"),
            s(:str, "<p>Para of text</p>\n"),
            s(:str, "<p>Remove this and figs order differently</p>\n"),
            s(:str, "<section>\n"),
            s(:str, "<figure src=\"fig3\"/>\n"),
            s(:str, "</section>\n"),
            s(:str, "<figure src=\"fig4\"/>\n"),
            s(:str, "</paper>\n"))),
        s(:def, :initialize,
          s(:args,
            s(:arg, :n)),
          s(:begin,
            s(:ivasgn, :@doc,
              s(:send,
                s(:const,
                  s(:const, nil, :REXML), :Document), :new,
                s(:const, nil, :DOC))),
            s(:ivasgn, :@figs,
              s(:send,
                s(:const,
                  s(:const, nil, :REXML), :XPath), :match,
                s(:ivar, :@doc),
                s(:str, "//figure"))),
            s(:ivasgn, :@names,
              s(:block,
                s(:send,
                  s(:ivar, :@figs), :collect),
                s(:args,
                  s(:arg, :f)),
                s(:send,
                  s(:send,
                    s(:lvar, :f), :attributes), :[],
                  s(:str, "src")))),
            s(:zsuper))),
        s(:def, :test_fig1,
          s(:args),
          s(:send, nil, :assert_equal,
            s(:str, "fig1"),
            s(:send,
              s(:send,
                s(:send,
                  s(:ivar, :@figs), :[],
                  s(:int, 0)), :attributes), :[],
              s(:str, "src")))),
        s(:def, :test_fig2,
          s(:args),
          s(:send, nil, :assert_equal,
            s(:str, "fig2"),
            s(:send,
              s(:send,
                s(:send,
                  s(:ivar, :@figs), :[],
                  s(:int, 1)), :attributes), :[],
              s(:str, "src")))),
        s(:def, :test_fig3,
          s(:args),
          s(:send, nil, :assert_equal,
            s(:str, "fig3"),
            s(:send,
              s(:send,
                s(:send,
                  s(:ivar, :@figs), :[],
                  s(:int, 2)), :attributes), :[],
              s(:str, "src")))),
        s(:def, :test_fig4,
          s(:args),
          s(:send, nil, :assert_equal,
            s(:str, "fig4"),
            s(:send,
              s(:send,
                s(:send,
                  s(:ivar, :@figs), :[],
                  s(:int, 3)), :attributes), :[],
              s(:str, "src"))))))))
