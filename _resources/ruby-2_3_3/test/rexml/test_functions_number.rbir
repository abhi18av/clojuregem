s(:begin,
  s(:send, nil, :require,
    s(:str, "rexml/document")),
  s(:send, nil, :require,
    s(:str, "test/unit")),
  s(:send, nil, :require,
    s(:str, "rexml/functions")),
  s(:module,
    s(:const, nil, :REXMLTests),
    s(:class,
      s(:const, nil, :TC_Rexml_Functions_Number),
      s(:const,
        s(:const,
          s(:const, nil, :Test), :Unit), :TestCase),
      s(:begin,
        s(:def, :test_functions_number_int,
          s(:args),
          s(:begin,
            s(:lvasgn, :telem,
              s(:send,
                s(:const,
                  s(:const, nil, :REXML), :Element), :new,
                s(:str, "elem"))),
            s(:send,
              s(:lvar, :telem), :text=,
              s(:str, "9")),
            s(:send, nil, :assert_equal,
              s(:int, 9),
              s(:send,
                s(:const,
                  s(:const, nil, :REXML), :Functions), :number,
                s(:lvar, :telem))))),
        s(:def, :test_functions_number_float,
          s(:args),
          s(:begin,
            s(:lvasgn, :telem,
              s(:send,
                s(:const,
                  s(:const, nil, :REXML), :Element), :new,
                s(:str, "elem"))),
            s(:send,
              s(:lvar, :telem), :text=,
              s(:str, "10.4")),
            s(:send, nil, :assert_equal,
              s(:float, 10.4),
              s(:send,
                s(:const,
                  s(:const, nil, :REXML), :Functions), :number,
                s(:lvar, :telem))))),
        s(:def, :test_functions_number_negative_int,
          s(:args),
          s(:begin,
            s(:lvasgn, :telem,
              s(:send,
                s(:const,
                  s(:const, nil, :REXML), :Element), :new,
                s(:str, "elem"))),
            s(:send,
              s(:lvar, :telem), :text=,
              s(:str, "-9")),
            s(:send, nil, :assert_equal,
              s(:int, -9),
              s(:send,
                s(:const,
                  s(:const, nil, :REXML), :Functions), :number,
                s(:lvar, :telem))))),
        s(:def, :test_functions_number_negative_float,
          s(:args),
          s(:begin,
            s(:lvasgn, :telem,
              s(:send,
                s(:const,
                  s(:const, nil, :REXML), :Element), :new,
                s(:str, "elem"))),
            s(:send,
              s(:lvar, :telem), :text=,
              s(:str, "-9.13")),
            s(:send, nil, :assert_equal,
              s(:float, -9.13),
              s(:send,
                s(:const,
                  s(:const, nil, :REXML), :Functions), :number,
                s(:lvar, :telem)))))))))
