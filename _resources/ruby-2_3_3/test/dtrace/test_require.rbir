s(:begin,
  s(:send, nil, :require_relative,
    s(:str, "helper")),
  s(:if,
    s(:defined?,
      s(:const,
        s(:const, nil, :DTrace), :TestCase)),
    s(:module,
      s(:const, nil, :DTrace),
      s(:class,
        s(:const, nil, :TestRequire),
        s(:const, nil, :TestCase),
        s(:begin,
          s(:def, :test_require_entry,
            s(:args),
            s(:begin,
              s(:lvasgn, :probe,
                s(:dstr,
                  s(:str, "ruby$target:::require-entry\n"),
                  s(:str, "{\n"),
                  s(:str, "  printf(\"%s %s %d\\n\", copyinstr(arg0), copyinstr(arg1), arg2);\n"),
                  s(:str, "}\n"))),
              s(:block,
                s(:send, nil, :trap_probe,
                  s(:lvar, :probe),
                  s(:send, nil, :ruby_program)),
                s(:args,
                  s(:arg, :d_file),
                  s(:arg, :rb_file),
                  s(:arg, :saw)),
                s(:begin,
                  s(:lvasgn, :required,
                    s(:block,
                      s(:send,
                        s(:block,
                          s(:send,
                            s(:lvar, :saw), :map),
                          s(:args,
                            s(:arg, :s)),
                          s(:send,
                            s(:lvar, :s), :split)), :find_all),
                      s(:args,
                        s(:mlhs,
                          s(:arg, :required),
                          s(:arg, :_))),
                      s(:send,
                        s(:lvar, :required), :==,
                        s(:str, "dtrace/dummy")))),
                  s(:send, nil, :assert_equal,
                    s(:int, 10),
                    s(:send,
                      s(:lvar, :required), :length)))))),
          s(:def, :test_require_return,
            s(:args),
            s(:lvasgn, :probe,
              s(:dstr,
                s(:str, "ruby$target:::require-return\n"),
                s(:str, "{\n"),
                s(:str, "  printf(\"%s\\n\", copyinstr(arg0));\n"),
                s(:str, "}\n")))),
          s(:send, nil, :private),
          s(:def, :ruby_program,
            s(:args),
            s(:str, "10.times { require 'dtrace/dummy' }"))))), nil))
