s(:begin,
  s(:send, nil, :require,
    s(:str, "test/unit")),
  s(:class,
    s(:const, nil, :TestCondition),
    s(:const,
      s(:const,
        s(:const, nil, :Test), :Unit), :TestCase),
    s(:def, :test_condition,
      s(:args),
      s(:begin,
        s(:gvasgn, :$x,
          s(:str, "0")),
        s(:and,
          s(:send,
            s(:gvar, :$x), :==,
            s(:gvar, :$x)),
          s(:send, nil, :assert,
            s(:true))),
        s(:and,
          s(:send,
            s(:gvar, :$x), :!=,
            s(:gvar, :$x)),
          s(:send, nil, :assert,
            s(:false))),
        s(:or,
          s(:send,
            s(:gvar, :$x), :==,
            s(:gvar, :$x)),
          s(:send, nil, :assert,
            s(:false))),
        s(:or,
          s(:send,
            s(:gvar, :$x), :!=,
            s(:gvar, :$x)),
          s(:send, nil, :assert,
            s(:true)))))))
